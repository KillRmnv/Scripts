
sudo apt update && sudo apt install docker.io

# Запуск OpenWebUI
docker run -d -p 3000:8080 --add-host=host.docker.internal:host-gateway -v open-webui:/app/backend/data --name open-webui --restart always ghcr.io/open-webui/open-webui:main




## В редакторе systemd нужно добавить переменную окружения. Вставьте между маркерами:

### Editing /etc/systemd/system/ollama.service.d/override.conf
### Anything between here and the comment below will become the new contents of the file

[Service]
Environment="OLLAMA_HOST=0.0.0.0:11434"

### Lines below this comment will be discarded



## -------------------------------------------------------------------------

sudo systemctl daemon-reload
sudo systemctl restart ollama

## -------------------------------------------------------------------------
## Проверьте, что порт теперь на всех интерфейсах:

sudo ss -tlnp | grep 11434
# должно быть 0.0.0.0:11434 или :::11434, не 127.0.0.1
  
  
## -------------------------------------------------------------------------
## нужно глянуть другие способы извлечения изображений из PDF.Сейчас используется Docling,но мб есть что помощнее для этого железа.Для интерпретатора кода стоит jupiter,мб поменять.
docker compose up -d
## --------------------------------------------------------------

## далее экспорт конфига или вручную настройка

OpenRouter:https://openrouter.ai/api/v1


